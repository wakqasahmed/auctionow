'use strict';

// Configuring the Chat module
angular.module('auction').run(['Menus',
  function(Menus) {
  	/*
    // Set top bar menu items
    Menus.addMenuItem('topbar', {
      title: 'Current Auction',
      state: 'auction'
    });
    */
  }
]);